---
title: 'Руководство по Markdown'
date: '2019-10-11'
language: ru
tags: ['github', 'guide']
draft: false
summary: 'Шпаргалка по Markdown с примерами синтаксиса: заголовки, списки, изображения, таблицы и другое на основе GitHub Flavored Markdown.'
---

# Введение

Парсинг Markdown и MDX поддерживается с помощью `unified` и других пакетов remark и rehype. `next-mdx-remote` позволяет нам обрабатывать файлы `.mdx` и `.md` более гибко, не вмешиваясь в webpack.

Используется GitHub Flavored Markdown. `mdx-prism` предоставляет возможности подсветки синтаксиса для блоков кода. Вот демонстрация того, как всё это выглядит.

Следующая шпаргалка по Markdown адаптирована из: https://guides.github.com/features/mastering-markdown/

# Что такое Markdown?

Markdown — это способ оформления текста в интернете. Вы управляете отображением документа: форматирование слов как полужирного или курсивного текста, добавление изображений и создание списков — это лишь несколько вещей, которые можно делать с помощью Markdown. В основном, Markdown — это обычный текст с добавлением нескольких неалфавитных символов, таких как `#` или `*`.

# Руководство по синтаксису

Вот обзор синтаксиса Markdown, который вы можете использовать где угодно на GitHub.com или в своих собственных текстовых файлах.

## Заголовки

```
# Это тег h1

## Это тег h2

#### Это тег h4
```

# Это тег h1

## Это тег h2

#### Это тег h4

## Выделение текста

```
_Этот текст будет курсивом_

**Этот текст будет полужирным**

_Вы **можете** комбинировать их_
```

_Этот текст будет курсивом_

**Этот текст будет полужирным**

_Вы **можете** комбинировать их_

## Списки

### Ненумерованный

```
- Элемент 1
- Элемент 2
  - Элемент 2a
  - Элемент 2b
```

- Элемент 1
- Элемент 2
  - Элемент 2a
  - Элемент 2b

### Нумерованный

```
1. Элемент 1
1. Элемент 2
1. Элемент 3
   1. Элемент 3a
   1. Элемент 3b
```

1. Элемент 1
1. Элемент 2
1. Элемент 3
   1. Элемент 3a
   1. Элемент 3b

## Изображения

```
![Логотип GitHub](https://github.githubassets.com/images/modules/logos_page/GitHub-Mark.png)
Формат: ![Альтернативный текст](ссылка)
```

![Логотип GitHub](https://github.githubassets.com/images/modules/logos_page/GitHub-Mark.png)

## Ссылки

```
http://github.com - автоматически!
[GitHub](http://github.com)
```

http://github.com - автоматически!
[GitHub](http://github.com)

## Цитаты (Блоки)

```
Как сказал Kanye West:

> Мы живём в будущем, поэтому
> настоящее — это наше прошлое.
```

Как сказал Kanye West:

> Мы живём в будущем, поэтому
> настоящее — это наше прошлое.

## Встроенный код

```
Я думаю, вам стоит использовать
элемент `<addr>` здесь.
```

Я думаю, вам стоит использовать
элемент `<addr>` здесь.

## Подсветка синтаксиса

Вот пример того, как можно использовать подсветку синтаксиса с [GitHub Flavored Markdown](https://help.github.com/articles/basic-writing-and-formatting-syntax/):

````
```js:fancyAlert.js
function fancyAlert(arg) {
  if (arg) {
    $.facebox({ div: '#foo' })
  }
}
```
````

И вот как это выглядит — красиво раскрашено со стилизованными заголовками кода!

```js:fancyAlert.js
function fancyAlert(arg) {
  if (arg) {
    $.facebox({ div: '#foo' })
  }
}
```

## Сноски

```
Вот простая сноска[^1]. И немного дополнительного текста после неё.

[^1]: Моя ссылка.
```

Вот простая сноска[^1]. И немного дополнительного текста после неё.

[^1]: Моя ссылка.

## Списки задач

```
- [x] требуется синтаксис списка (поддерживается любой ненумерованный или нумерованный список)
- [x] это выполненный элемент
- [ ] это невыполненный элемент
```

- [x] требуется синтаксис списка (поддерживается любой ненумерованный или нумерованный список)
- [x] это выполненный элемент
- [ ] это невыполненный элемент

## Таблицы

Вы можете создавать таблицы, собирая список слов и разделяя их дефисами `-` (для первой строки), а затем разделяя каждый столбец вертикальной чертой `|`:

```
| Первый заголовок          | Второй заголовок           |
| ------------------------- | -------------------------- |
| Содержимое ячейки 1       | Содержимое ячейки 2        |
| Содержимое в первом столбце | Содержимое во втором столбце |
```

| Первый заголовок            | Второй заголовок             |
| --------------------------- | ---------------------------- |
| Содержимое ячейки 1         | Содержимое ячейки 2          |
| Содержимое в первом столбце | Содержимое во втором столбце |

## Зачёркивание

Любое слово, обёрнутое двумя тильдами (как `~~это~~`), будет отображаться ~~зачёркнутым~~.
